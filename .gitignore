# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Node (only if you later add tooling — none needed to deploy this static site)
node_modules/
npm-debug.log*

# Local env / secrets — never commit these
.env
.env.local
*.local.yaml
